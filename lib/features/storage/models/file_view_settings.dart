enum FileViewMode { allFolders, files, folders }

enum FileLayout { list, grid }

enum FileSortOption {
  title,
  date,
  playedTime,
  status,
  length,
  size,
  resolution,
  path,
  frameRate,
  type,
}
